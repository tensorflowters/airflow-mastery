# Solution 0.1: Basic pyproject.toml after `uv init` and `uv add apache-airflow`
# This is the minimal configuration after completing Exercise 0.1

[project]
name = "airflow-learning"
version = "0.1.0"
description = "Learning Apache Airflow 3.x"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "apache-airflow>=3.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
